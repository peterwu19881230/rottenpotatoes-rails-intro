<!--  This file is app/views/movies/index.html.erb -->
<h2>All Movies</h2>

<%#  Part 1: Start here... %>


<table class="table table-striped col-md-12" id="movies">
  <thead>
    <%= form_tag movies_path, :method => :get do %>
    Include:
    <% @all_ratings.each do |rating| %>
    <%= rating %>
    <%= check_box_tag "ratings[#{rating}]" %>
    <% end %>
    <%= submit_tag 'Refresh', :class => 'btn btn-primary' %>
    <% end %>
    <tr>
      <th><div id="title_header" style="background-color:wheat"><%= link_to "Movie Title", :sort => "title" %></div></th>
      <th>Rating</th>
      <th><div id="release_date_header" style="background-color:wheat"><%= link_to "Release Date", :sort => "release_date" %></div></th>
      <th>More Info</th>
    </tr>
  </thead>
  <tbody>
    <% @movies.each do |movie| %>
      <tr>
        <td>
          <%= movie.title %>
        </td>
        <td>
          <%= movie.rating %>
        </td>
        <td>
          <%= movie.release_date %>
        </td>
        <td>
          <%= link_to "More about #{movie.title}", movie_path(movie) %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to 'Add new movie', new_movie_path, :class => 'btn btn-primary' %>
